import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex',
    height: '100%',
    paddingHorizontal: 20,
  },
  containerStyle: {
    flex: 1,
    alignItems: 'center',
  },
});
